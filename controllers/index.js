const condition = require('./condition')
const treeData = require('./treeData')
const report = require('./report')
const health = require('./health')

module.exports = [condition, treeData, report, health]
